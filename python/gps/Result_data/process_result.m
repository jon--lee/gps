function [out] = process_result(line_val)
    % Function handles an array of clawed data from each iteration of
    % samples based on any logic that we want to implement

    out = sum(line_val);    % Implements a simple summation for now\
end